# UI primitives

These files come from the shadcn/ui CLI and were adapted for this store.
shadcn/ui and Base UI are MIT licensed, lucide-react is ISC licensed.
The primitives keep the CLI's double-quote style so they stay diffable against `shadcn add`, and the lint style rules exclude this directory.
